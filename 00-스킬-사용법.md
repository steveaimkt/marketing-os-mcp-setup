# 클론한 다음 — 스킬 사용법

> 이 저장소는 **설치 스킬 모음**입니다. 클론만 하면 아무 일도 일어나지 않고,
> 스킬을 Claude Code 에 인식시켜야 `/mcp설치-전체` 같은 명령이 작동합니다.
> 아래 3분이면 끝납니다.

---

## 0. 클론

```bash
git clone https://github.com/steveaimkt/marketing-os-mcp-setup.git
cd marketing-os-mcp-setup
cp .env.example .env
```

---

## 1. 스킬 등록 (한 줄)

```bash
bash setup-skills.sh
```

저장소 안의 모든 `SKILL.md` 를 `.claude/skills/` 로 복사합니다. 출력 예:

```
  ✓ /mcp설치-전체
  ✓ /mcp설치-firecrawl
  ✓ /mcp설치-naver
  ...
완료 · 17 개 스킬 등록
```

**어느 폴더에서든 쓰고 싶다면** (개인 스킬로 등록):
```bash
bash setup-skills.sh --user      # ~/.claude/skills/ 에 설치
```

> **Windows**: PowerShell 말고 **Git Bash** 에서 실행하세요.
> Git Bash 가 없으면 <https://git-scm.com/download/win> 에서 Git 설치 시 함께 깔립니다.

---

## 2. 실행

```bash
claude
```

그리고 아래 중 **아무거나** 입력하면 됩니다.

### 방법 A · 슬래시 명령 (등록 후)

```
/mcp설치-전체
```

`/` 만 치면 등록된 스킬 목록이 뜹니다. 방향키로 골라도 됩니다.

| 명령 | 하는 일 |
|---|---|
| `/mcp설치-전체` | 12개 MCP 순차 설치 (모드 A/B/C 선택형) |
| `/mcp설치-firecrawl` | 경쟁사 스크래핑 |
| `/mcp설치-naver` | 네이버 데이터랩 + 검색광고 |
| `/mcp설치-google-sheets` | 구글 시트 |
| `/mcp설치-ga4` | GA4 웹 분석 |
| `/mcp설치-youtube` | 유튜브 데이터 |
| `/mcp설치-figma` | 피그마 |
| `/mcp설치-higgsfield` | 이미지·영상 생성 |
| `/mcp설치-buffer` | SNS 예약 발행 |
| `/mcp설치-ads` | 메타·구글 광고 |
| `/mcp설치-notion` | 노션 |
| `/bot-build` | 디스코드 봇 |

### 방법 B · 그냥 말하기 (권장)

슬래시를 몰라도 됩니다. 스킬에 트리거 문구가 등록돼 있어 자연어로 호출됩니다.

```
MCP 전체 설치하자
파이어크롤 MCP 설치
네이버 MCP 설치하자
```

### 방법 C · 등록 없이 파일만 지정 (스킬 등록이 귀찮을 때)

```
03-firecrawl/mcp설치-firecrawl/SKILL.md 읽고 그대로 설치 진행해줘
```

`setup-skills.sh` 를 안 돌려도 이 방식은 바로 됩니다.

---

## 3. 처음이라면 이 순서

```
1) node --version          ← ⚠️ 여기서 막히면 00-Node설치.md 부터
2) bash setup-skills.sh
3) claude
4) "MCP 전체 설치하자"     ← 모드 A 선택
```

`/mcp설치-전체` 는 3가지 모드로 물어봅니다.

| 모드 | 언제 |
|---|---|
| **A 실제 설치** | 처음 셋업. 이미 깔린 건 건너뜀 |
| **B 교육 시연** | 이미 다 깔려 있어도 전 과정을 하나씩 보고 싶을 때 (강의·복습용, 비파괴) |
| **C 점검만** | 지금 뭐가 연결됐는지 표만 보고 종료 |

---

## 4. 12개를 다 깔 필요는 없습니다

본인이 안 쓰는 채널은 건너뛰세요. 최소 조합 추천:

| 목적 | 필요한 것 |
|---|---|
| 경쟁사·시장 조사 | Firecrawl + 네이버 데이터랩 |
| 성과 리포트 | GA4 + Google Sheets |
| 광고 운영 | Meta·Google Ads + 네이버 검색광고 |
| 콘텐츠 발행 | Buffer + Notion |

---

## 5. 잘 안 될 때

| 증상 | 먼저 볼 곳 |
|---|---|
| `/` 를 눌러도 스킬이 안 보임 | `bash setup-skills.sh` 실행했는지 + Claude Code 재시작 |
| MCP 가 `failed to connect` · 목록에 없음 | [00-Node설치.md](00-Node설치.md) ★ 실패 1순위 |
| 키는 넣었는데 인증 오류 | Claude Code 완전 종료(⌘Q) 후 재시작 |
| 커넥터 3종(Figma·Notion·Higgsfield)만 되고 나머지 전부 실패 | Node.js 미설치 확정 → [00-Node설치.md](00-Node설치.md) |
| 그 외 | 각 도구 폴더의 `mcp설치-*/SKILL.md` 맨 아래 트러블슈팅 표 |

> 스킬을 `.claude/skills/` 로 복사한 뒤에는 스킬 안의 상대 링크(`../../00-Node설치.md`)가
> 어긋납니다. 해당 문서는 **저장소 루트**에 있으니 그쪽을 보세요.

---

## 참고

- 전체 사전 준비물: [00-사전준비물.md](00-사전준비물.md)
- Node.js 설치·진단: [00-Node설치.md](00-Node설치.md)
- MCP 개념: [00-MCP란-무엇인가/](00-MCP란-무엇인가/)
- 12개 한눈에: [MCP-11개-종합정리.md](MCP-11개-종합정리.md)
